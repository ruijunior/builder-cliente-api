spring:
  datasource:
    url: jdbc:mysql://localhost/builder_test?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password:
    hikari:
      maximum-pool-size: 1
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
